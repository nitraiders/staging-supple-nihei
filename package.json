{
  "name": "nk-group-render-migration-notice",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Render app that shows a migration notice for the old NK Group URL.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
